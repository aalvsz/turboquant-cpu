version: '3.8'

services:
  turboquant-dev:
    build:
      context: .
      dockerfile: docker/dev.dockerfile
    container_name: turboquant-dev
    volumes:
      - .:/workspace/turboquant
    command: /bin/bash
